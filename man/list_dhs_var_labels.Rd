% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_var_labels.R
\name{list_dhs_var_labels}
\alias{list_dhs_var_labels}
\title{List DHS variables and their haven labels by name pattern}
\usage{
list_dhs_var_labels(
  data,
  pattern,
  regex = FALSE,
  only_observed = FALSE,
  duplicate_label = TRUE
)
}
\arguments{
\item{data}{A data frame (typically a DHS recode such as the KR file
read by \code{\link[=dhs_read]{dhs_read()}}). Columns may be plain vectors or
haven-labelled vectors; only the name and \code{label} attribute are
inspected.}

\item{pattern}{Character scalar. Either a prefix (default) or a regex
(when \code{regex = TRUE}). Must be non-empty.}

\item{regex}{Logical. If \code{FALSE} (default), \code{pattern} is treated as a
prefix and the matched regex is \code{paste0("^", pattern, "[a-z0-9]+$")}.
If \code{TRUE}, \code{pattern} is used as the full regex.}

\item{only_observed}{Logical. If \code{TRUE}, drop variables with no
non-NA values, or - for numeric 0/1 columns - no observed \code{1}.
Default \code{FALSE}.}

\item{duplicate_label}{Logical. If \code{TRUE} (default), add a
\code{duplicate_label} logical column flagging variables whose label is
shared with at least one other matched variable.}
}
\value{
A tibble with columns:
\itemize{
\item \code{variable}: matched column name.
\item \code{label}: haven \code{label} attribute (or \code{NA_character_} if
absent).
\item \code{n_nonmissing}: number of non-NA values in the column.
\item \code{n_ones}: number of values equal to \code{1} (or \code{NA} for
non-numeric columns).
\item \code{duplicate_label} (only if \code{duplicate_label = TRUE}): logical,
\code{TRUE} if another matched variable has the same non-NA label.
}
Variables are returned in the order they appear in \code{data}. If no
variables match, an empty tibble with the same columns is returned
and a \code{cli} info message is emitted.
}
\description{
Generic utility to inspect a (typically DHS) data frame and return a tidy
inventory of every column whose name matches a given prefix or regex,
together with its haven \code{label} attribute. This is useful when building
specs for \code{custom_csb_indicator} (or any indicator that routes columns by
label / variable name), since DHS recodes share many label conventions
across surveys but the exact set of populated columns varies by country
and round.
}
\details{
Two matching modes are supported:

\itemize{
\item Prefix mode (default, \code{regex = FALSE}): the helper matches columns
whose names start with \code{pattern} followed by one or more lowercase
letters or digits. For example, \code{pattern = "h32"} will match \code{h32a},
\code{h32x}, \code{h32a1}, but not \code{h32_recoded} or \code{h32}.
\item Regex mode (\code{regex = TRUE}): \code{pattern} is used directly as a
regular expression. For example, \code{pattern = "^ml13[a-z]$"} will match
ITN-source variables \code{ml13a}..\code{ml13h}.
}

Optionally, the helper can:

\itemize{
\item Mark variables whose haven label is shared with another variable
in the same inventory (\code{duplicate_label = TRUE}). This is the
situation that motivates routing a \code{custom_csb_indicator} spec by
variable \strong{name} rather than by label (e.g. h32e and h32n in some
surveys both carry the label "Fever/cough: comm.health wrkr").
\item Drop variables that have no observed \code{1} (or no non-NA values
at all for non-binary columns) via \code{only_observed = TRUE}. This
keeps the inventory focused on slots actually populated by the
survey.
}

The function never modifies \code{data}; it only reads column names and
the \code{label} attribute (as set by \code{haven::read_dta()} or
\code{sntmethods::dhs_read()}).
}
\examples{
\dontrun{
  kr_data <- sntmethods::dhs_read(
    path        = path_dhs_parquet,
    file_type   = "KR",
    survey_type = "DHS"
  )

  # All h32* treatment-seeking source columns
  list_dhs_var_labels(kr_data, "h32")

  # Only h32 columns with at least one observed `1`, flagging duplicates
  list_dhs_var_labels(kr_data, "h32", only_observed = TRUE)

  # ITN source columns ml13a..ml13h via regex
  list_dhs_var_labels(kr_data, "^ml13[a-z]$", regex = TRUE)
}

}
