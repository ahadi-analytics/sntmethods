% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_calc_act.R
\name{calc_act_dhs}
\alias{calc_act_dhs}
\title{Calculate ACT Treatment Indicators from DHS Data}
\usage{
calc_act_dhs(
  dhs_kr,
  dhs_kr_raw = NULL,
  survey_vars = list(cluster = "v021", weight = "v005", stratum = "v022", age = "hw1",
    fever = "h22", alive = "b5", act = "ml13e", test = "ml13a"),
  region_var = NULL,
  gps_data = NULL,
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM"),
  shapefile = NULL,
  admin_level = NULL,
  join_nearest = TRUE,
  dhs_pr = NULL,
  indicators = NULL,
  ci_method = "logit"
)
}
\arguments{
\item{dhs_kr}{DHS Children's Recode (KR) dataset (data.frame or tibble).
If read via \code{dhs_read()}, haven labels may be standardised. Supply
\code{dhs_kr_raw} for accurate ACT variable detection.}

\item{dhs_kr_raw}{Optional un-standardised KR dataset (e.g., from
\code{arrow::read_parquet()}) with original survey-specific haven labels.
When provided, ACT and antimalarial variables are detected from its
labels, and any extra variables (e.g., ml13aa, ml13da) are copied
into the analysis dataset. This is the "two-pass" approach needed when
\code{dhs_read()} strips country-specific drug names.}

\item{survey_vars}{Named list mapping DHS variable names. Required keys:
\itemize{
\item \code{cluster}: Cluster/PSU ID (default: "v021")
\item \code{weight}: Survey weight (default: "v005")
\item \code{stratum}: Stratum variable (default: "v022")
\item \code{age}: Child's age in months (default: "hw1")
\item \code{fever}: Had fever in last 2 weeks (default: "h22")
\item \code{alive}: Child alive (default: "b5")
\item \code{act}: ACT variable(s) (default: "ml13e";
auto-detected from labels)
\item \code{test}: Test-positive filter variable (default: "ml13a")
}}

\item{region_var}{Optional column name for subnational
grouping (e.g., "v024").}

\item{gps_data}{Optional DHS GE (Geographic) dataset
with cluster coordinates.}

\item{gps_vars}{Named list for GE variables: cluster, lat, lon.}

\item{shapefile}{Optional sf object with administrative boundaries.}

\item{admin_level}{Character vector of admin columns from shapefile.}

\item{join_nearest}{Logical; if TRUE, assigns unmatched clusters to nearest
admin unit. Default: TRUE.}

\item{dhs_pr}{Optional DHS Person Recode (PR) for febrile RDT indicators.}

\item{indicators}{Character vector of indicator names to compute. If NULL
(default), computes all indicators from \code{\link[=act_dictionary]{act_dictionary()}}.}

\item{ci_method}{Method for confidence intervals. Default: "logit".}
}
\value{
Named list of tibbles, one per admin level:
\describe{
\item{\code{adm0}}{National-level estimates (always present)}
\item{\code{adm1}}{Admin-1 estimates (when \code{region_var} or shapefile used)}
\item{\code{adm2}}{Admin-2 estimates (when shapefile with adm2 used)}
}
Each tibble contains columns:
\itemize{
\item \code{survey_id}: Survey identifier (e.g., "TG2017DHS")
\item \code{iso3}: ISO 3166-1 alpha-3 country code (e.g., "TGO")
\item \code{iso2}: DHS 2-letter country code (e.g., "TG")
\item \code{survey_type}: Survey type ("DHS", "MIS", or "AIS")
\item \code{survey_year}: Survey year (integer)
\item \code{adm0}: Country name in UPPERCASE (e.g., "TOGO")
\item \code{adm1}, \code{adm2}: Admin names in UPPERCASE (subnational tabs only)
\item \code{type}: Analysis type ("survey_weighted")
\item \code{geo_source}: Source of admin names ("survey" or "gps")
\item \code{point}: Survey-weighted proportion (0-1 scale)
\item \code{ci_l}, \code{ci_u}: Lower and upper 95\\% CI bounds
\item \code{numerator}: Unweighted numerator count
(has_act == 1 in filtered subgroup)
\item \code{denominator}: Unweighted denominator count
(condition-filtered subgroup size)
\item \code{indicator}: Indicator name in Title Case
(e.g., "Act Antimalarial")
\item \code{indicator_code}: Short indicator code (e.g., "act_am")
\item \code{numerator_description}: Description of numerator
\item \code{denominator_description}: Description of denominator
\item \code{denominator_code}: Short code for the denominator subpopulation
}
}
\description{
Computes the full set of (World Malaria Report) ACT treatment indicators
from DHS Children's Recode (KR) data. Returns survey-weighted proportions
with logit confidence intervals in standardized long format.
}
\details{
Computes up to 12 ACT indicators following DHS methodology. Each indicator
measures the proportion of febrile U5 children receiving ACT within a
specific subpopulation defined by care-seeking behaviour and antimalarial
receipt. See \code{\link[=act_dictionary]{act_dictionary()}} for the full indicator list.

The function uses three internal helpers:
\itemize{
\item \code{.prepare_act_data()} for ACT variable detection
and febrile U5 filtering
\item \code{.classify_csb_from_h32()} for care-seeking behaviour classification
\item Antimalarial composite built from ml13/h37 drug series
}
}
\examples{
\dontrun{
# Two-pass approach (recommended): read standardised + raw
kr <- sntmethods::dhs_read(path, file_type = "KR", ...)
kr_raw <- arrow::read_parquet(parquet_path)
act <- calc_act_dhs(dhs_kr = kr, dhs_kr_raw = kr_raw)

# By region
act <- calc_act_dhs(dhs_kr = kr, dhs_kr_raw = kr_raw, region_var = "v024")

# With GE spatial join
act <- calc_act_dhs(
  dhs_kr = kr, dhs_kr_raw = kr_raw,
  gps_data = ge_data,
  shapefile = admin_sf,
  admin_level = "adm1"
)

# Subset of indicators
act <- calc_act_dhs(
  dhs_kr = kr, dhs_kr_raw = kr_raw,
  indicators = c("ACT_ANTIMALARIAL", "ACT_PUBLIC_ANTIMALARIAL")
)
}

}
\seealso{
\code{\link[=act_dictionary]{act_dictionary()}} for indicator definitions,
\code{\link[=calc_act_mbg]{calc_act_mbg()}} for cluster-level MBG inputs
}
