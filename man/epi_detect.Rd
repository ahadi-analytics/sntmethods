% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_detect.R
\name{epi_detect}
\alias{epi_detect}
\title{Run outbreak detectors on a weekly count series}
\usage{
epi_detect(
  data,
  methods,
  date_col = "date",
  count_col = "cases",
  group_col = NULL,
  denominator_col = NULL,
  delay_col = NULL,
  label_col = NULL,
  target_range = NULL,
  baseline_range = NULL,
  transform = c("none", "log1p", "anscombe", "freeman_tukey"),
  detrend = FALSE,
  use_rates = FALSE,
  aggregation = c("week", "epi_week_4", "month"),
  method_params = list()
)
}
\arguments{
\item{data}{Tidy data frame with at minimum a date column and a non-
negative count column. See \strong{Input contract} below.}

\item{methods}{Character vector of detector ids to apply. Run
\code{\link[=available_detectors]{available_detectors()}} to see what is registered.}

\item{date_col, count_col}{Column names; defaults \code{"date"} and
\code{"cases"}.}

\item{group_col}{Optional column name identifying panel groups
(district, facility, stratum). Each group is detected independently
in v1.1; hierarchical pooling is deferred to v2.}

\item{denominator_col}{Optional column name with catchment population
or expected counts. Required when \code{use_rates = TRUE}.}

\item{delay_col}{Optional column name with reporting delay in weeks.
Currently triggers a warning (delay correction is deferred to v2 via
\code{surveillance::bodaDelay}).}

\item{label_col}{Optional column name with known outbreak labels
(logical). Carried through to the run object so \code{\link[=epi_evaluate]{epi_evaluate()}} can
use it without re-joining data.}

\item{target_range, baseline_range}{Each accepts integer row indices, a
logical vector of length \code{nrow(data)}, or a \code{Date} vector. Defaults:
\verb{target_range = last full calendar year},
\verb{baseline_range = everything earlier}.}

\item{transform}{Variance-stabilising transformation: one of
\code{"none"}, \code{"log1p"}, \code{"anscombe"}, \code{"freeman_tukey"}.}

\item{detrend}{If \code{TRUE}, remove a linear time trend from cases before
detection. Methods with their own trend handling (Farrington,
trending, arima) should not be double-detrended; a warning is
emitted if they are.}

\item{use_rates}{If \code{TRUE}, divide cases by \code{denominator_col} before
detection.}

\item{aggregation}{Temporal grain: \code{"week"}, \code{"epi_week_4"} (four-week
rolling sum), or \code{"month"}.}

\item{method_params}{Named list of per-method parameter overrides; e.g.
\code{list(threshold = list(k = 3), ears_c1 = list(baseline = 12))}.}
}
\value{
An \code{epi_detection_run} S3 object (printable; coerce to tibble
with \code{tibble::as_tibble()}) carrying:
\itemize{
\item \verb{$predictions} — long tibble with \code{method}, \code{group_id},
\code{date}, \code{alarm}, \code{score}, \code{upper_threshold}, \code{failed},
\code{error_message}.
\item \verb{$preprocessing} — list capturing transform, detrend,
use_rates, aggregation, target/baseline ranges, and the raw
input data, so LOO and operating-point analyses can re-derive
detection deterministically.
\item \verb{$methods} — the requested method ids.
\item \verb{$call} — captured call for printing.
}
}
\description{
Apply one or more registered outbreak detectors to a tidy weekly count
series and return a per-method, per-week tibble of alarms and scores
wrapped in an \code{epi_detection_run} S3 object that downstream
\code{\link[=epi_evaluate]{epi_evaluate()}}, \code{\link[=epi_ensemble]{epi_ensemble()}}, and \code{\link[=epi_recommend]{epi_recommend()}} verbs consume.
}
\section{Input contract}{

\describe{
\item{\code{date}}{Date, regular weekly spacing within each group.}
\item{\code{cases}}{Non-negative numeric count.}
\item{\code{group_id}}{Optional character/factor panel id.}
\item{\code{outbreak_label}}{Optional logical; used by \code{\link[=epi_evaluate]{epi_evaluate()}}.}
\item{\code{denominator}}{Optional positive numeric population/expected.}
\item{\code{report_delay_weeks}}{Optional non-negative integer; warned
about (delay correction deferred to v2).}
}
}

\section{Failure isolation}{

If a detector errors on a given group, the framework returns rows with
\code{alarm = NA}, \code{score = NA_real_}, \code{upper_threshold = NA_real_},
\code{failed = TRUE}, and the captured \code{error_message} rather than crashing
the run. Downstream verbs (\code{\link[=epi_evaluate]{epi_evaluate()}}, \code{\link[=epi_ensemble]{epi_ensemble()}},
\code{\link[=epi_recommend]{epi_recommend()}}) skip or downweight failed methods consistently.
}

\examples{
\donttest{
set.seed(1)
weekly <- tibble::tibble(
  date = seq.Date(as.Date("2018-01-07"), by = "week",
                  length.out = 260L),
  cases = rpois(260L, 8)
)
run <- epi_detect(
  weekly,
  methods = c("threshold", "ears_c1"),
  target_range = which(format(weekly$date, "\%Y") == "2022"),
  baseline_range = which(format(weekly$date, "\%Y") < "2022")
)
run$predictions
}

}
\seealso{
\code{\link[=available_detectors]{available_detectors()}}, \code{\link[=register_detector]{register_detector()}},
\code{\link[=epi_evaluate]{epi_evaluate()}}, \code{\link[=epi_ensemble]{epi_ensemble()}}, \code{\link[=epi_recommend]{epi_recommend()}}.
}
