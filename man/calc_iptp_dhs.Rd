% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_iptp.R
\name{calc_iptp_dhs}
\alias{calc_iptp_dhs}
\title{Calculate IPTp Coverage from DHS Data (standardized long-format output)}
\usage{
calc_iptp_dhs(
  dhs_ir,
  survey_vars = list(cluster = "v001", weight = "v005", stratum = "v022", adm1 = "v024",
    adm2 = NULL, interview_cmc = "v008", birth_cmc = "b3_01", birth_age_months =
    "b19_01", sp_taken = "m49a_1", sp_doses = "ml1_1"),
  birth_window_months = 24,
  region_var = NULL,
  gps_data = NULL,
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM"),
  shapefile = NULL,
  admin_level = NULL,
  join_nearest = TRUE,
  ci_method = "logit"
)
}
\arguments{
\item{dhs_ir}{DHS Individual Recode dataset (IR) in tidy format.}

\item{survey_vars}{Named list mapping DHS variable names.}

\item{birth_window_months}{Maximum age of most recent birth in months.
Default 24 (DHS standard).}

\item{region_var}{Optional column name (character string) in \code{dhs_ir} to use
as the subnational grouping variable (e.g., \code{"v024"} for region).}

\item{gps_data}{Optional DHS GE (GPS) cluster dataset used to attach
admin-unit labels when \code{shapefile} is supplied. Default \code{NULL}.}

\item{gps_vars}{Named list mapping cluster/lat/lon column names in
\code{gps_data}. Defaults to the standard DHS GE names
(\code{DHSCLUST}, \code{LATNUM}, \code{LONGNUM}).}

\item{shapefile}{Optional \code{sf} polygon dataset whose attributes carry
admin labels for the cluster-to-admin spatial join. When \code{NULL}
(default) the spatial join step is skipped.}

\item{admin_level}{Character vector of admin column names in \code{shapefile}
to retain (e.g. \code{c("adm1", "adm2")}). Default \code{NULL} (use all).}

\item{join_nearest}{Logical. If \code{TRUE} (default), clusters that fall
outside any polygon are re-assigned to the nearest polygon. If
\code{FALSE}, unmatched clusters are left as \code{NA}.}

\item{ci_method}{CI method for svyciprop. Default: "logit".}
}
\value{
Named list with \code{adm0} tibble and optionally \code{adm1} tibble in
standardized long format.
}
\description{
Computes IPTp 1+/2+/3+/4+ and exact-dose 1/2/3 coverage indicators
nationally and optionally by subnational region, returning the standardized
\code{list(adm0, adm1)} output.
}
\examples{
\dontrun{
# Basic usage
result <- calc_iptp_dhs(dhs_ir = ir_data)

# With subnational estimates
result <- calc_iptp_dhs(
  dhs_ir = ir_data,
  region_var = "v024"
)
}

}
