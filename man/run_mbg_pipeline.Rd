% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mbg_pipeline.R
\name{run_mbg_pipeline}
\alias{run_mbg_pipeline}
\title{Run MBG Indicator Pipeline}
\usage{
run_mbg_pipeline(
  country_iso3,
  adm0_sf,
  adm1_sf,
  adm2_sf,
  adm3_sf = NULL,
  pop_raster,
  path_dhs_parquet,
  table_out_path,
  raster_out_path,
  intermediate_out_path,
  pop_raster_u5 = NULL,
  pop_raster_1_2 = NULL,
  pop_raster_5_10 = NULL,
  pop_raster_10_20 = NULL,
  pop_raster_20plus = NULL,
  pop_raster_wra = NULL,
  country_iso2 = NULL,
  survey_year = NULL,
  min_year = "2005",
  survey_type = NULL,
  indicators = c("pfpr", "itn", "irs", "anc", "csb", "anemia", "iptp", "epi", "fever",
    "antimalarial"),
  aggregation_level = c("adm2", "adm3"),
  run_mbg = TRUE,
  save_rasters = TRUE,
  save_cluster_points = TRUE,
  cluster_points_out_path = NULL,
  cache = TRUE,
  csb_priority_method = c("all", "first", "public", "private"),
  custom_csb_indicator = NULL,
  verbose = TRUE,
  debug = FALSE
)
}
\arguments{
\item{country_iso3}{Three-letter ISO country code (e.g., "bdi").}

\item{adm0_sf}{sf object with country boundary.}

\item{adm1_sf}{sf object with ADM1 boundaries.}

\item{adm2_sf}{sf object with ADM2 boundaries.}

\item{adm3_sf}{sf object with ADM3 boundaries (optional). Required when
\code{aggregation_level = "adm3"}. Should contain columns for adm3 names and
parent adm2/adm1/adm0 linkages.}

\item{pop_raster}{Total population raster(s). Can be:
\itemize{
\item Named list with years as names and file paths as values:
\code{list("2019" = "path/to/pop_2019.tif", "2020" = "path/to/pop_2020.tif")}
\item Single file path (used for all years)
\item Already-loaded SpatRaster object (used for all years)
}}

\item{path_dhs_parquet}{Path to DHS parquet archive.}

\item{table_out_path}{Output directory for tables (CSV, XLSX).}

\item{raster_out_path}{Output directory for prediction rasters.}

\item{intermediate_out_path}{Output directory for cached intermediate outputs
(aggregation tables, ID rasters).}

\item{pop_raster_u5}{Under-5 population raster(s) (optional). Same format as
\code{pop_raster}. Used for indicators targeting children 0-59 months (PfPR,
fever, CSB, ACT, antimalarial, anemia, U5MR, SMC, ITN use U5).
If NULL, falls back to \code{pop_raster}.}

\item{pop_raster_1_2}{Population raster for children 12-23 months (optional).
Same format as \code{pop_raster}. Used for EPI (immunization) indicators.
Falls back to \code{pop_raster_u5}, then \code{pop_raster} if NULL.}

\item{pop_raster_5_10}{Population raster for ages 5-10 (optional). Same
format as \code{pop_raster}. Used for \code{use_itn_5_10}. Falls back to
\code{pop_raster} if NULL.}

\item{pop_raster_10_20}{Population raster for ages 10-20 (optional). Same
format as \code{pop_raster}. Used for \code{use_itn_10_20}. Falls back to
\code{pop_raster} if NULL.}

\item{pop_raster_20plus}{Population raster for ages 20+ (optional). Same
format as \code{pop_raster}. Used for \code{use_itn_20plus}. Falls back to
\code{pop_raster} if NULL.}

\item{pop_raster_wra}{Women of reproductive age (15-49) population raster
(optional). Same format as \code{pop_raster}. Used for ANC, IPTp, and ITN
use among pregnant women. Falls back to \code{pop_raster} if NULL.}

\item{country_iso2}{Two-letter DHS country code (e.g., "BU"). If NULL
(default), derived automatically from \code{country_iso3} using the
\code{countrycode} package.}

\item{survey_year}{Survey year(s) to process. Can be:
\itemize{
\item NULL: Process ALL available surveys with GPS data
\item Single integer: Process only that year (e.g., 2016)
\item Integer vector: Process specific years (e.g., c(2012, 2016))
}}

\item{min_year}{Minimum survey year to include. Surveys before this year
will be excluded. Useful for filtering out older surveys that may lack
key indicators. Default: NULL (no minimum).}

\item{survey_type}{Survey type(s) to process. Can be:
\itemize{
\item NULL (default): Auto-detect all available survey types (DHS, MIS, etc.)
\item Single string: Process only that type (e.g., "DHS")
\item Character vector: Process specific types (e.g., c("DHS", "MIS"))
}}

\item{indicators}{Character vector of indicator categories to process:
\itemize{
\item "pfpr": Parasite prevalence
\item "itn": ITN ownership/access/use
\item "irs": IRS coverage
\item "anc": ANC attendance
\item "csb": Care-seeking behavior
\item "act": ACT treatment (case management)
\item "anemia": Anemia prevalence
\item "iptp": IPTp doses
\item "epi": EPI vaccination
\item "u5mr": Under-5 mortality
\item "smc": SMC receipt
\item "fever": Fever prevalence (U5)
\item "antimalarial": Any antimalarial treatment (febrile U5)
\item "wealth": Wealth quintile distribution (proportion in poorest/richest)
\item "eff_cm": Effective coverage of case management (derived;
auto-adds "csb" and "act" as dependencies)
}}

\item{aggregation_level}{Primary aggregation level for MBG outputs. One of:
\itemize{
\item "adm2": Aggregate to ADM2 level (default)
\item "adm3": Aggregate to ADM3 level (requires \code{adm3_sf})
}}

\item{run_mbg}{Logical. If TRUE, runs MBG models. Default: TRUE.}

\item{save_rasters}{Logical. If TRUE, saves output rasters. Default: TRUE.}

\item{save_cluster_points}{Logical. If TRUE (default), saves the
coordinate-level (cluster-point) datasets produced by
\verb{calc_*_mbg()} to disk in addition to the aggregated admin-level
outputs. Set to FALSE to skip saving cluster-point files.}

\item{cluster_points_out_path}{Output directory for cluster-point
(coordinate-level) datasets. If NULL (default), falls back to
\code{table_out_path}. Use this to keep the per-cluster point files in a
separate location from the combined admin-level outputs.}

\item{cache}{Logical. If TRUE (default), reuses cached intermediate outputs
(aggregation tables, ID rasters) when available. Set to FALSE to force
regeneration of all intermediate outputs.}

\item{csb_priority_method}{Character, one of "all" (default), "first",
"public", or "private". Controls how overlapping care-seeking records
are resolved in the CSB (and wealth-stratified CSB) indicators so each
individual is assigned to at most one sector:
\itemize{
\item "all": Keep WHO methodology; overlaps allowed (csb_public and
csb_private can both be 1 for the same child).
\item "first": Take the first recurring h32 source visited per child
(alphabetical h32 order: h32a, h32b, ..., h32x). Mutually exclusive.
\item "public": Public-sector priority - if any public/CHW care,
classify as public; otherwise private if any private; otherwise none.
\item "private": Private-sector priority - if any private care,
classify as private; otherwise public if any public; otherwise none.
}
With non-"all" values, csb_public + csb_private + csb_none sums to 100\%.}

\item{custom_csb_indicator}{Optional named list defining a user-specified,
mutually-exclusive care-seeking partition fitted in addition to the
built-in CSB family. When supplied, three derived indicators are
produced: \code{<name>_dhis} (sought care at any user-listed DHIS
source), \code{<name>_nondhis} (sought care at any user-listed
non-DHIS source and not at any DHIS source), and \code{<name>_untreat}
(did not seek care at any user-listed source). The list must have
four character fields:
\itemize{
\item \code{name}: A short alphanumeric prefix used to build the
three derived indicator codes (e.g. \code{"foo"} -> \code{foo_dhis},
\code{foo_nondhis}, \code{foo_untreat}).
\item \code{dhis_locs}: Character vector of h32 entries that map to
the DHIS bucket. Each entry may be either an h32 variable name
(e.g. \code{"h32a"}) or a haven label string (case-insensitive).
The two styles can be mixed in the same vector; variable-name
matches take precedence over label matches when an h32 column
is referenced both ways.
\item \code{nondhis_locs}: Character vector for the non-DHIS bucket
(same dual-style semantics as \code{dhis_locs}).
\item \code{untreat_locs}: Character vector for the untreated bucket
(same dual-style semantics as \code{dhis_locs}).
}
When \code{custom_csb_indicator} is supplied, the partition is
activated automatically: the \code{name} is added to
\code{indicators} unless it (or one of the three derived codes) is
already listed. The custom triple is always mutually exclusive at
the child level (priority dhis > nondhis > untreat); admin-level
estimates are rescaled to sum to 100\% per admin unit.
Default: NULL (disabled).}

\item{verbose}{Logical. If TRUE, prints detailed progress. Default: TRUE.}

\item{debug}{Logical. If TRUE, prints additional
diagnostic messages for troubleshooting.
Default: FALSE.}
}
\value{
A list containing:
\itemize{
\item final_dataset: Named list with \code{adm0}, \code{adm1}, and \code{adm2} (or \code{adm3})
tibbles in long format. Each tibble has standard columns: survey_id, iso3,
iso2, survey_type, survey_year, adm0, adm1, adm2, type, geo_source,
point, ci_l, ci_u, numerator, denominator,
survey_numerator, survey_denominator, n_survey_clusters,
indicator, indicator_code, numerator_description,
denominator_description.
\item mbg_estimates: MBG predictions at the specified aggregation level
(if MBG was run)
\item cluster_data: Raw cluster-level data
\item raster_paths: Paths to saved rasters
\item survey_metadata: Survey collection dates and metadata
}
}
\description{
Orchestrates the full MBG processing pipeline for DHS indicators.
See indicator-specific methodology files at
\url{https://github.com/ahadi-analytics/sntmethods/tree/master/inst/methods}
}
\details{
Pipeline steps:
\enumerate{
\item Discovers available surveys
\item Loads survey data
\item Prepares cluster-level data for each indicator
\item Runs MBG models (if enabled)
\item Generates outputs (rasters, CSVs, maps)
}
}
\examples{
\dontrun{
results <- run_mbg_pipeline(
  country_iso3 = "bdi",
  adm0_sf = adm0,
  adm1_sf = adm1,
  adm2_sf = adm2,
  pop_raster = list("2016" = "/path/to/bdi_ppp_2016.tif"),
  path_dhs_parquet = "path/to/parquet",
  table_out_path = "path/to/output/tables",
  raster_out_path = "path/to/output/rasters",
  intermediate_out_path = "path/to/output/intermediate",
  survey_year = 2016,
  indicators = c("pfpr", "itn", "csb"),
  cache = TRUE
)
}

}
