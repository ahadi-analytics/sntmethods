% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_detect.R
\name{register_detector}
\alias{register_detector}
\title{Register a custom outbreak detector}
\usage{
register_detector(name, constructor, fit_method, predict_method)
}
\arguments{
\item{name}{Character scalar method id used in
\code{epi_detect(methods = ...)}. Must be unique within the active R
session.}

\item{constructor}{Function that builds an \code{epi_detector} object.}

\item{fit_method}{Function implementing \code{fit_detector} for this
detector's subclass.}

\item{predict_method}{Function implementing \code{predict_detector} for this
detector's subclass; must return the four-column contract.}
}
\value{
Invisibly returns \code{name}.
}
\description{
Adds a user-defined detector to the package-internal registry so it can
be referenced by name inside \code{\link[=epi_detect]{epi_detect()}}. The framework supplies the
failure-isolation shell, contract validation, and downstream integration;
authors only need to honour the four-column predict contract.
}
\section{Detector contract}{

Every registered detector consists of three callables:
\itemize{
\item \code{constructor(...)} — returns an \code{epi_detector} object (typically via
\code{\link[=new_epi_detector]{new_epi_detector()}}).
\item \code{fit_method(detector, baseline_data, ...)} — an S3 method on
\verb{epi_detector_<name>} that returns the fitted detector.
\item \code{predict_method(detector, target_data, ...)} — an S3 method on
\verb{epi_detector_<name>} that returns a tibble with \strong{exactly} the four
columns \code{date}, \code{alarm}, \code{score}, \code{upper_threshold}. Anything else
(e.g. \code{failed}, \code{error_message}, \code{method}, \code{group_id}) is added by the
framework — do not return those columns yourself.
}
}

\examples{
\dontrun{
my_constructor <- function(k = 2) {
  new_epi_detector(method = "my_z", params = list(k = k))
}

my_fit <- function(detector, baseline_data, ...) {
  detector$baseline_mean <- mean(baseline_data$cases, na.rm = TRUE)
  detector$baseline_sd <- stats::sd(baseline_data$cases, na.rm = TRUE)
  detector$fitted <- TRUE
  detector
}

my_predict <- function(detector, target_data, ...) {
  thresh <- detector$baseline_mean +
    detector$params$k * detector$baseline_sd
  tibble::tibble(
    date = target_data$date,
    alarm = target_data$cases > thresh,
    score = (target_data$cases - detector$baseline_mean) /
      detector$baseline_sd,
    upper_threshold = thresh
  )
}

register_detector("my_z", my_constructor, my_fit, my_predict)
"my_z" \%in\% available_detectors()
}

}
\seealso{
\code{\link[=available_detectors]{available_detectors()}}, \code{\link[=new_epi_detector]{new_epi_detector()}},
\code{\link[=fit_detector]{fit_detector()}}, \code{\link[=predict_detector]{predict_detector()}}.
}
