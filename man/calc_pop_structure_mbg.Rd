% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_demographics.R
\name{calc_pop_structure_mbg}
\alias{calc_pop_structure_mbg}
\title{Cluster-level Population Age Structure for MBG}
\usage{
calc_pop_structure_mbg(
  dhs_pr,
  gps_data,
  indicators = c("prop_u5", "prop_ov5", "prop_u5_urban", "prop_u5_rural",
    "prop_ov5_urban", "prop_ov5_rural"),
  survey_vars = list(cluster = "hv001", age = "hv105", dejure = "hv102", residence =
    "hv025"),
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM")
)
}
\arguments{
\item{dhs_pr}{DHS Person Recode (PR) data frame -- one row per household
member. Must contain the cluster, age, and de jure residence variables
named in \code{survey_vars}.}

\item{gps_data}{DHS GPS (GE) data frame with cluster coordinates.}

\item{indicators}{Character vector of indicator codes to compute. Any of
\code{"prop_u5"} / \code{"prop_ov5"} (whole resident population) and the
residence-stratified variants \code{"prop_u5_urban"}, \code{"prop_u5_rural"},
\code{"prop_ov5_urban"}, \code{"prop_ov5_rural"}. Default: all six. The stratified
variants split clusters by \code{hv025} (each DHS EA is classified urban or
rural), so an urban indicator is present only at urban clusters and a
rural indicator only at rural clusters.}

\item{survey_vars}{Named list mapping logical names to PR variable names:
\code{cluster} (default \code{"hv001"}), \code{age} (default \code{"hv105"}, age in years),
\code{dejure} (default \code{"hv102"}, usual resident flag), and \code{residence}
(default \code{"hv025"}, type of place of residence; 1 = urban, 2 = rural).}

\item{gps_vars}{Named list mapping GPS variable names: \code{cluster}
(default \code{"DHSCLUST"}), \code{lat} (\code{"LATNUM"}), \code{lon} (\code{"LONGNUM"}).}
}
\value{
A named list keyed by indicator code. Each element is a tibble with
columns \code{cluster_id}, \code{indicator} (numerator count), \code{samplesize}
(denominator), \code{x}, \code{y}, matching the shape consumed by the MBG pipeline.
Returns \code{NULL} if no valid clusters could be prepared.
}
\description{
Computes cluster-level age-structure proportions from a DHS Person Recode
(PR / household member roster), formatted as binomial cluster inputs for
model-based geostatistics. The numerator is the count of de jure household
members in the target age band; the denominator is all de jure household
members with a known age.
}
\details{
Unlike the malaria indicators (PfPR, ITN use) which restrict to \emph{de facto}
residents (who slept in the household the previous night), age-structure
indicators use \emph{de jure} residence (\code{hv102}, usual residents) so the
estimate reflects the resident population rather than visitors.

Age values are taken from \code{hv105} (age in completed years). DHS special
codes for unknown age (96-99) and the \code{hv105 = 95} top-code are handled by
keeping values \code{0}-\code{95} (with \code{95} treated as "95+", i.e. 5+) and dropping
codes above \code{95} from both numerator and denominator.

Because the indicator is a property of the whole resident population, its
aggregation weight in the pipeline (\code{pop_type}) is \code{"all"} (total
population), not the under-5 raster -- weighting the under-5 proportion by
the under-5 population would be circular.
}
\examples{
\dontrun{
pr <- sntutils::read("path/to/pr.parquet")
ge <- sntutils::read("path/to/ge.parquet")

# Proportion under 5 and 5+ per cluster
pop <- calc_pop_structure_mbg(dhs_pr = pr, gps_data = ge)
pop$prop_u5

# Via the pipeline
results <- run_mbg_pipeline(
  country_iso3 = "gin",
  indicators = "pop_structure",
  ...
)
}

}
\seealso{
\code{\link[=run_mbg_pipeline]{run_mbg_pipeline()}} for automated pipeline processing
}
