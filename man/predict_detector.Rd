% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_detect.R
\name{predict_detector}
\alias{predict_detector}
\title{Generate alarms for target data}
\usage{
predict_detector(detector, target_data, ...)
}
\arguments{
\item{detector}{A fitted \code{epi_detector} object.}

\item{target_data}{A tibble with \code{date} and \code{cases} columns covering the
detection window.}

\item{...}{Reserved for method-specific arguments.}
}
\value{
A tibble with the four-column contract described above.
}
\description{
Generic for producing the per-week alarm series from a fitted detector.
}
\details{
\strong{Invariant contract} — every \code{predict_detector} method must return a
tibble with \strong{exactly} these four columns, in order:\tabular{lll}{
   Column \tab Type \tab Meaning \cr
   \code{date} \tab Date \tab matches \code{target_data$date} \cr
   \code{alarm} \tab logical \tab binary alarm decision \cr
   \code{score} \tab numeric \tab continuous signal (e.g. observed/expected ratio) \cr
   \code{upper_threshold} \tab numeric \tab cutoff value used to produce \code{alarm} \cr
}


The framework's failure-isolation shell adds \code{method}, \code{group_id},
\code{failed}, and \code{error_message} columns at the \code{\link[=epi_detect]{epi_detect()}} level on top
of this four-column contract, so third-party detector authors registered
via \code{\link[=register_detector]{register_detector()}} only need to honour the four columns above.
}
