% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_evaluate.R
\name{epi_evaluate}
\alias{epi_evaluate}
\title{Evaluate an outbreak detection run}
\usage{
epi_evaluate(
  detection_run,
  labels = NULL,
  simulation = NULL,
  metrics = "all",
  cost = NULL,
  loo_method = TRUE,
  ensemble_strategy = c("majority", "weighted", "probability"),
  ensemble_k = NULL,
  k_sweep = TRUE,
  operating_points = TRUE,
  f_beta = 1
)
}
\arguments{
\item{detection_run}{An object of class \code{epi_detection_run}.}

\item{labels}{Optional column name or logical vector.}

\item{simulation}{Optional simulation specification list. See details.}

\item{metrics}{Either \code{"all"} or a character vector of metric names.}

\item{cost}{Optional list with \code{cost_per_fp} and \code{cost_per_fn} numeric
scalars; enables expected-cost computation.}

\item{loo_method}{If \code{TRUE}, run the leave-one-method-out engine.
Requires ground truth (\code{labels} or \code{simulation}).}

\item{ensemble_strategy}{Strategy used internally by LOO and k-sweep:
\code{"majority"}, \code{"weighted"}, or \code{"probability"}.}

\item{ensemble_k}{For \code{strategy = "majority"}; defaults to
\code{ceiling(n_methods / 2)}.}

\item{k_sweep}{If \code{TRUE}, sweep \code{k} across \verb{1..n_methods} and report
precision-recall at each k.}

\item{operating_points}{If \code{TRUE} and \code{simulation} is supplied, build
the operating-point table.}

\item{f_beta}{Numeric scalar for the F-beta metric.}
}
\value{
An object of class \code{epi_evaluation} carrying \verb{$per_method},
\verb{$loo_method}, \verb{$operating_points}, \verb{$ensemble_sweep}, \verb{$cost},
\verb{$fp_shoulder}, \verb{$unlabelled}, \verb{$mode}, \verb{$metadata}.
}
\description{
Compute per-method metrics, leave-one-method-out (LOO) deltas,
operating-point tables, k-sweep, cost-aware metrics, and false-positive
shoulder timing for an \link{epi_detection_run} returned by \code{\link[=epi_detect]{epi_detect()}}.
}
\details{
Three operating modes are selected automatically by what the caller
supplies:
\itemize{
\item \strong{labelled} — pass \code{labels} (a column name in the original
data, or a logical vector aligned with \code{target_range}).
\item \strong{simulation-injected} — pass \code{simulation} (a list with
\code{n_iterations}, \code{magnitudes}, \code{durations}, \code{shape},
\code{grace_period}, optional \code{seed}). Synthetic outbreaks are added
to a quiet baseline and detection is re-run.
\item \strong{unlabelled qualitative} — pass neither. Limited diagnostics
(pairwise method agreement, alarm density, persistence) only.
}
}
\examples{
\donttest{
set.seed(1)
weekly <- tibble::tibble(
  date = seq.Date(as.Date("2018-01-07"), by = "week",
                  length.out = 260L),
  cases = rpois(260L, 8),
  outbreak_label = FALSE
)
weekly$outbreak_label[200:210] <- TRUE
weekly$cases[200:210] <- weekly$cases[200:210] + 20

run <- epi_detect(
  weekly,
  methods = c("threshold", "ears_c1"),
  target_range = 209:260,
  baseline_range = 1:208
)
eval_res <- epi_evaluate(
  run,
  labels = "outbreak_label",
  loo_method = TRUE,
  k_sweep = TRUE
)
eval_res$per_method
}

}
\seealso{
\code{\link[=epi_detect]{epi_detect()}}, \code{\link[=epi_ensemble]{epi_ensemble()}}, \code{\link[=epi_recommend]{epi_recommend()}}.
}
