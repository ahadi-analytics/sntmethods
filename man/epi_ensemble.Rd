% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_ensemble.R
\name{epi_ensemble}
\alias{epi_ensemble}
\title{Combine per-method alarms from an \code{epi_detection_run} into an ensemble.}
\usage{
epi_ensemble(
  detection_run,
  strategy = c("majority_vote", "weighted_vote", "score_average"),
  weights = NULL,
  k = NULL,
  na_handling = c("drop", "as_false"),
  methods_subset = NULL
)
}
\arguments{
\item{detection_run}{An \code{epi_detection_run} object (the output of
\code{\link[=epi_detect]{epi_detect()}}).}

\item{strategy}{One of \code{"majority_vote"}, \code{"weighted_vote"},
\code{"score_average"}.}

\item{weights}{Optional. Either \code{NULL} (equal weighting), a named numeric
vector keyed by detector method, or an \code{epi_evaluation} object.}

\item{k}{Optional integer. Minimum vote count to raise an alarm under
\code{majority_vote}. Defaults to \code{ceiling(M/2)} where \code{M} is the number of
non-failed methods at that (group, date).}

\item{na_handling}{One of \code{"drop"} (default) or \code{"as_false"}. Controls how
failed detector cells (alarm = NA) are treated when combining.}

\item{methods_subset}{Optional character vector restricting the ensemble
to a subset of the detectors present in \code{detection_run}.}
}
\value{
An \code{epi_ensemble} object: a list with elements
\itemize{
\item \verb{$predictions} — tibble with \code{method = "ensemble"}, \code{group_id},
\code{date}, \code{alarm}, \code{score}, \code{upper_threshold = NA}, \code{failed},
\code{error_message}.
\item \verb{$strategy}, \verb{$k}, \verb{$na_handling}, \verb{$weights} — configuration.
\item \verb{$source_run} — the source \code{epi_detection_run} (for plotting).
\item \verb{$call} — matched call.
}
}
\description{
Three ensemble strategies are supported (\code{majority_vote}, \code{weighted_vote},
\code{score_average}). Failed detector cells are governed by \code{na_handling}.
Weights may be derived from an \code{epi_evaluation} object so that methods
which hurt the LOO ensemble are down-weighted to zero.
}
\seealso{
\code{\link[=epi_detect]{epi_detect()}}, \code{\link[=epi_evaluate]{epi_evaluate()}}, \code{\link[=epi_recommend]{epi_recommend()}}.
}
