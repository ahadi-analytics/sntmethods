% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_pfpr.R
\name{prep_pfpr_mbg}
\alias{prep_pfpr_mbg}
\title{Prepare Single PfPR Indicator for MBG}
\usage{
prep_pfpr_mbg(
  dhs_pr,
  gps_data,
  indicator = "pfpr_rdt_u5",
  age_min = 6,
  age_max = 59,
  survey_vars = list(cluster = "hv001", age = "hc1", present = "hv103", mother = "hv042",
    rdt = "hml35", mic = "hml32"),
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM")
)
}
\arguments{
\item{dhs_pr}{DHS Person Records dataset (data.frame or tibble).}

\item{gps_data}{DHS GPS dataset with cluster coordinates.}

\item{indicator}{Single indicator code (e.g., \code{"pfpr_rdt_u5"}).
Also accepts legacy \code{test_type} values (\code{"rdt"}, \code{"mic"})
combined with \code{age_min}/\code{age_max}.}

\item{age_min}{Minimum age in months (inclusive). Default: 6.
Only used when \code{indicator} is a legacy test_type name.}

\item{age_max}{Maximum age in months (inclusive). Default: 59.
Only used when \code{indicator} is a legacy test_type name.}

\item{survey_vars}{Named list mapping DHS variable names. Required keys:
\itemize{
\item cluster: Cluster ID (default: "hv001")
\item age: Age in months (default: "hc1")
\item present: Present in household (default: "hv103")
\item mother: Mother listed in household (default: "hv042")
\item rdt: RDT result variable (default: "hml35")
\item mic: Microscopy result variable (default: "hml32")
}}

\item{gps_vars}{Named list for GPS variable mapping.}
}
\value{
A tibble with columns: cluster_id, indicator, samplesize, x, y
}
\description{
Simplified function to prepare a single PfPR indicator for MBG. Returns
a single data.table rather than a list.
}
