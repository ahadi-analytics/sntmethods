% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_iptp.R
\name{calc_iptp_mbg}
\alias{calc_iptp_mbg}
\title{Prepare IPTp Data for MBG Analysis}
\usage{
calc_iptp_mbg(
  dhs_ir,
  gps_data,
  indicators = c("iptp_1plus", "iptp_2plus", "iptp_3plus"),
  birth_window_months = 36,
  survey_vars = list(cluster = "v001", interview_date = "v008", birth_date = "b3_01",
    sp_doses = "ml1_1", sp_taken = "m49a_1"),
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM")
)
}
\arguments{
\item{dhs_ir}{DHS Individual Recode (IR) or Children's Recode (KR) dataset.}

\item{gps_data}{DHS GPS dataset with cluster coordinates.}

\item{indicators}{Character vector of indicators to calculate:
\itemize{
\item Cumulative:
\itemize{
\item "iptp_1plus": At least 1 dose
\item "iptp_2plus": At least 2 doses
\item "iptp_3plus": At least 3 doses (WHO recommendation)
\item "iptp_4plus": At least 4 doses (requires ml1_1 as sp_doses)
}
\item Exclusive:
\itemize{
\item "iptp_1only": Exactly 1 dose
\item "iptp_2only": Exactly 2 doses
\item "iptp_3only": Exactly 3 doses
}
}
Default: c("iptp_1plus", "iptp_2plus", "iptp_3plus").}

\item{birth_window_months}{Months to look back for births. Default: 36.}

\item{survey_vars}{Named list mapping DHS variable names.}

\item{gps_vars}{Named list for GPS variable mapping.}
}
\value{
A list of data.tables (one per indicator).
}
\description{
Prepares cluster-level Intermittent Preventive Treatment in pregnancy (IPTp)
data for MBG analysis. Calculates both cumulative (1+, 2+, 3+) and
exclusive (exactly 1, exactly 2, exactly 3) dose categories.
}
\details{
Supports both IR (Individual Recode) and KR (Children's Recode) formats.
The function automatically detects the file type and adjusts variable names.

Methodology: \url{https://github.com/ahadi-analytics/sntmethods/blob/master/inst/methods/iptp_dhs.yml}

No ANC attendance restriction is applied; the denominator is all women
with a birth in the analysis window and a valid SP response
(\code{sp_doses <= 7}). The default \code{survey_vars$sp_doses = "ml1_1"} is
the dose count variable (0-7). If \code{ml1_1} is not available, the helper
falls back to \code{sp_taken} (\code{"m49a_1"}, binary 0/1), in which case only
IPTp 1+ will produce meaningful results.
}
\examples{
\dontrun{
iptp_mbg <- calc_iptp_mbg(
  dhs_ir = ir_data,
  gps_data = gps_data,
  indicators = c("iptp_2plus", "iptp_3plus")
)
}

}
\seealso{
\code{\link[=calc_iptp_dhs]{calc_iptp_dhs()}} for survey-weighted estimates
}
