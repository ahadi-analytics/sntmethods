% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_calc_malaria_dx.R
\name{calc_malaria_dx_dhs}
\alias{calc_malaria_dx_dhs}
\title{Calculate Malaria Diagnostic Testing from DHS Data (Standardized)}
\usage{
calc_malaria_dx_dhs(
  dhs_kr,
  survey_vars = list(cluster = "v021", weight = "v005", stratum = "v022", age = "hw1",
    fever = "h22", malaria_dx = "h47"),
  region_var = NULL,
  gps_data = NULL,
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM"),
  shapefile = NULL,
  admin_level = NULL,
  join_nearest = TRUE,
  ci_method = "logit"
)
}
\arguments{
\item{dhs_kr}{DHS children's recode (KR) dataset (data.frame or tibble).}

\item{survey_vars}{Named list mapping DHS variable names. Required keys:
\itemize{
\item \code{cluster}: Cluster/PSU ID (default: "v021")
\item \code{weight}: Survey weight (default: "v005")
\item \code{stratum}: Stratum variable (default: "v022")
\item \code{age}: Child's age in months (default: "hw1")
\item \code{fever}: Had fever in last 2 weeks (default: "h22")
\item \code{malaria_dx}: Blood taken for malaria test (default: "h47")
}}

\item{region_var}{Optional column name in \code{dhs_kr} to use as grouping
variable (e.g., "v024" for region).}

\item{gps_data}{Optional DHS GPS dataset with cluster coordinates.}

\item{gps_vars}{Named list for GPS variables (cluster, lat, lon).}

\item{shapefile}{Optional sf object with administrative boundaries.}

\item{admin_level}{Character vector of admin columns from shapefile
(e.g., c("adm1", "adm2")).}

\item{join_nearest}{Logical; if TRUE, assigns clusters outside polygons
to nearest admin unit. Default: TRUE.}

\item{ci_method}{CI method for svyciprop. Default: "logit".}
}
\value{
Named list with \code{adm0} (national) and optionally \code{adm1} (regional)
tibbles in standardized long format.
}
\description{
Estimates the proportion of febrile children under 5 who had blood taken
for malaria testing. Returns standardized long-format output as
\code{list(adm0, adm1)}.
}
\seealso{
\code{\link[=malaria_dx_dictionary]{malaria_dx_dictionary()}} for indicator definitions,
\code{\link[=calc_malaria_dx_dhs_core]{calc_malaria_dx_dhs_core()}} for backward-compatible wide output
}
