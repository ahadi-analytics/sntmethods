% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_archive_reader.R
\name{dhs_archive_surveys}
\alias{dhs_archive_surveys}
\title{List the surveys present in a DHS parquet archive}
\usage{
dhs_archive_surveys(
  path,
  country_code,
  file_type = "GE",
  refresh_index = FALSE
)
}
\arguments{
\item{path}{Root parquet directory.}

\item{country_code}{DHS two-letter country code.}

\item{file_type}{Recode used for discovery. Defaults to \code{"GE"}, since a
survey without GPS cannot be modelled.}

\item{refresh_index}{Logical; rescan the archive.}
}
\value{
A tibble with \code{DHSYEAR}, \code{survey_type}, \code{type_source},
\code{survey_year_path}, \code{survey_id}, \code{n_rows}, \code{n_leaves}, \code{path}.
}
\description{
Builds one row per survey by indexing the archive's leaves and reading
their identity fields, rather than by reading a whole recode into
memory and calling \code{distinct()} on it.

Survey type is resolved in a fixed order and the source is reported, so
a caller can see how each label was arrived at:
\enumerate{
\item the \verb{survey_type=} partition value;
\item the in-file \code{survey_type} column (absent whenever survey_type was
used as a partition key, which is why it is second);
\item the \code{SurveyID} field carried by GE and GC extracts.
}

A survey whose type resolves to none of these is returned with
\code{survey_type = NA} and \code{type_source = "unresolved"}. It is never
silently relabelled \code{"DHS"}: doing so produces a survey that is
discovered and then loads zero rows for every other recode, because the
\code{survey_type} filter does apply to PR/HR/KR/IR.
}
