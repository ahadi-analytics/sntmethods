% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_recommend.R
\name{profile_series}
\alias{profile_series}
\title{Profile a count time-series for detector selection.}
\usage{
profile_series(data, date_col, count_col, group_col = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{date_col}{Bare or string column name with dates.}

\item{count_col}{Bare or string column name with counts.}

\item{group_col}{Optional bare/string column name. If supplied, a profile
is computed per group and returned in long format.}
}
\value{
A tibble with one row per group (or a single row if ungrouped)
containing: \code{group_id}, \code{n_obs}, \code{mean_count}, \code{median_count},
\code{low_count_fraction}, \code{dispersion}, \code{zero_inflation}, \code{missingness},
\code{seasonality_strength}, \code{trend_strength}.
}
\description{
Computes a small numeric profile of a univariate count series. Used by
\code{\link[=epi_recommend]{epi_recommend()}} but exported for users who want to interrogate a series
before selecting detectors manually.
}
\details{
\itemize{
\item \code{low_count_fraction} is the proportion of weeks with count < 5.
\item \code{dispersion} is \code{var(x) / mean(x)} (1 = Poisson, > 1 = overdispersed).
\item \code{seasonality_strength} is the maximum |ACF| over lags 4–12 weeks.
\item \code{trend_strength} is the absolute Kendall tau of count ~ date.
}
}
