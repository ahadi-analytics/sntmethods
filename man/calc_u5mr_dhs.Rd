% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_u5mr.R
\name{calc_u5mr_dhs}
\alias{calc_u5mr_dhs}
\title{Calculate U5MR from DHS Data (Standardized Long Format)}
\usage{
calc_u5mr_dhs(
  dhs_kr,
  survey_vars = list(cluster = "v021", weight = "v005", stratum = "v022", interview_date
    = "v008", birth_date = "b3", age_at_death = "b7"),
  period_years = 5,
  region_var = NULL,
  gps_data = NULL,
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM"),
  shapefile = NULL,
  admin_level = NULL,
  join_nearest = TRUE
)
}
\arguments{
\item{dhs_kr}{DHS Children's Recode (KR) dataset in tidy format.}

\item{survey_vars}{Named list mapping DHS variable names. See
\code{\link[=calc_u5mr_dhs_core]{calc_u5mr_dhs_core()}}.}

\item{period_years}{Years before survey to calculate rates (default: 5).}

\item{region_var}{Optional column name for subnational grouping
(e.g., "v024"). Auto-falls back to "v024" if no spatial params.}

\item{gps_data}{Optional DHS GPS dataset with cluster coordinates.}

\item{gps_vars}{Named list for GPS variables (cluster, lat, lon).}

\item{shapefile}{Optional sf object with administrative boundaries.}

\item{admin_level}{Character vector specifying aggregation levels.}

\item{join_nearest}{Logical; if TRUE, assigns clusters outside all
polygons to nearest administrative unit.}
}
\value{
Named list of tibbles:
\describe{
\item{\code{adm0}}{National-level estimates (always present)}
\item{\code{adm1}}{Admin-1 estimates (when region_var or shapefile used)}
}
Each tibble contains columns: survey_id, iso3, iso2, survey_type,
survey_year, adm0, adm1, type, geo_source, point, ci_l, ci_u,
numerator, denominator, indicator, indicator_code,
numerator_description, denominator_description, denominator_code.
}
\description{
Estimates under-5 mortality rate (U5MR) from DHS Children's Recode data
using the DHS.rates package. Returns results in standardized long format
with \code{list(adm0, adm1)} structure.
}
\details{
U5MR is computed via \code{DHS.rates::chmort()} (synthetic cohort life table
method), NOT via \code{svyciprop()}, because it is a rate per 1000 live births
rather than a proportion.
}
\seealso{
\code{\link[=u5mr_dictionary]{u5mr_dictionary()}} for indicator definitions,
\code{\link[=calc_u5mr_dhs_core]{calc_u5mr_dhs_core()}} for the legacy wide-format output
}
