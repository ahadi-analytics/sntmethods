% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_incidence.R
\name{calc_incidence}
\alias{calc_incidence}
\title{Calculate Malaria Incidence from Routine Health Facility Data (N0-N5)}
\usage{
calc_incidence(
  data,
  levels = c("N0", "N1", "N2", "N3"),
  hf_var = "hf_uid",
  adm0_var = NULL,
  adm1_var = "adm1",
  adm2_var = "adm2",
  adm3_var = NULL,
  date_var = "date",
  year_var = "year",
  pop_var = "pop",
  conf_var = "conf",
  test_var = "test",
  pres_var = "pres",
  tpr_var = "tpr",
  reprate_var = "reprate",
  cs_public_var = "cs_public",
  cs_private_var = "cs_private",
  cs_none_var = "cs_none",
  cs_none_divisor = 2,
  rate_multiplier = 1000,
  scale_factor = lifecycle::deprecated(),
  include_flags = FALSE,
  suffix = NULL,
  group_by = NULL,
  return_facility = FALSE
)
}
\arguments{
\item{data}{Routine health facility data at facility-month level
(data.frame or tibble). Must contain one row per facility per month.}

\item{levels}{Character vector specifying which incidence levels to calculate
(default: \code{c("N0", "N1", "N2", "N3")}). Can specify subset like \code{c("N0", "N1")} or include N4/N5 with \code{c("N0", "N1", "N2", "N3", "N4", "N5")}.}

\item{hf_var}{Column name for health facility unique identifier
(default: "hf_uid"). Set to \code{NULL} when data is already aggregated
at the admin level and has no facility identifier.}

\item{adm0_var}{Column name for national/country level. If NULL (default),
creates a single "country" value for all records.}

\item{adm1_var}{Column name for first administrative level/region
(default: "adm1").}

\item{adm2_var}{Column name for second administrative level/district
(default: "adm2").}

\item{adm3_var}{Column name for third administrative level/sub-district.
If NULL (default), adm3 column is not included in output.}

\item{date_var}{Column name for date of reporting period (default: "date").}

\item{year_var}{Column name for year (default: "year"). Used for year-level
aggregation in summary output.}

\item{pop_var}{Column name for population denominator (default: "pop").
Must be present in \code{data}.}

\item{conf_var}{Column name for number of confirmed malaria cases
(default: "conf").}

\item{test_var}{Column name for number of individuals tested
(default: "test").}

\item{pres_var}{Column name for number of presumed cases (default: "pres").}

\item{tpr_var}{Column name for test positivity rate (default: "tpr").
Must be present in \code{data} for N1/N2/N3 calculations.}

\item{reprate_var}{Column name for reporting rate (default: "reprate").
Must be present in \code{data} for N2/N3 calculations.}

\item{cs_public_var}{Column name for proportion seeking care at public
facilities (default: "cs_public"). Required for N3.}

\item{cs_private_var}{Column name for proportion seeking care at private
facilities (default: "cs_private"). Required for N3.}

\item{cs_none_var}{Column name for proportion seeking no care
(default: "cs_none"). Required for N3/N4/N5.}

\item{cs_none_divisor}{Numeric. Divisor applied to cs_none for N5 calculation
(default: 2). N5 uses \code{cs_none / cs_none_divisor} instead of \code{cs_none},
producing a more conservative estimate than N4.}

\item{rate_multiplier}{Denominator for incidence rate calculation
(default: 1000, for cases per 1,000 population).}

\item{scale_factor}{Deprecated. Use \code{rate_multiplier} instead.}

\item{include_flags}{Logical; if \code{TRUE}, includes all quality flag columns
in the output. If \code{FALSE} (default), returns only the core incidence
variables without flags.}

\item{suffix}{Character string to append to incidence column names
(default: NULL). If provided (e.g., \code{suffix = "u5"}), output columns
will be named \code{n0_cases_u5}, \code{n0_incidence_u5}, etc. Useful for
distinguishing outputs for different population groups.}

\item{group_by}{Character vector of additional column names to group by
(e.g. facility type, urban/rural). These columns are preserved through
aggregation and appear in output. Default NULL.}

\item{return_facility}{Logical; if \code{TRUE}, includes facility-level data
in the output with diagnostic flags. If \code{FALSE} (default), returns only
aggregated admin-level data. Useful for investigating data quality issues
before aggregation (e.g., why n1_cases < n0_cases).}
}
\value{
A named list with components:
\describe{
\item{monthly}{A named list with tibbles at each admin level (N0-N5):
\itemize{
\item \code{adm0}: National level monthly incidence (N0-N5)
\item \code{adm1}: First admin level (region) monthly incidence (N0-N5)
\item \code{adm2}: Second admin level (district) monthly incidence (N0-N5)
\item \code{adm3}: Third admin level monthly incidence (N0-N5, if \code{adm3_var} provided)
}
}
\item{annual}{A named list with tibbles at each admin level (N0-N5):
\itemize{
\item \code{adm0}: National level annual incidence (N0-N5)
\item \code{adm1}: First admin level (region) annual incidence (N0-N5)
\item \code{adm2}: Second admin level (district) annual incidence (N0-N5)
\item \code{adm3}: Third admin level annual incidence (N0-N5, if \code{adm3_var} provided)
}
}
\item{facility}{(Only if \code{return_facility = TRUE}) A named list with tibbles:
\itemize{
\item \code{monthly}: Facility-month level data
\item \code{annual}: Facility-year level data (if applicable)
}
}
}

Monthly and annual tibbles contain:
\itemize{
\item ID columns: \code{adm0}, \code{adm1}, \code{adm2}, \code{adm3} (depending on level)
\item Time columns: \code{year}, \code{month}, \code{date} (monthly) or \code{year} (annual)
\item \code{pop}: Population denominator (summed)
\item \code{conf}: Confirmed cases (summed)
\item \code{test}, \code{pres}, \code{tpr}: Testing data (summed, if N1+ calculated)
\item \code{reprate}: Reporting rate (summed, if N2+ calculated)
\item \code{cs_public}, \code{cs_private}, \code{cs_none}: Care-seeking proportions (if N3+ calculated)
\item \code{n0_cases}, \code{n0_incidence}: N0 (crude)
\item \code{n1_cases}, \code{n1_incidence}: N1 (testing-adjusted)
\item \code{n2_cases}, \code{n2_incidence}: N2 (reporting-adjusted)
\item \code{n3_cases}, \code{n3_incidence}: N3 (care-seeking-adjusted)
\item \code{n4_cases}, \code{n4_incidence}: N4 (public + non-seekers)
\item \code{n5_cases}, \code{n5_incidence}: N5 (conservative non-seekers)
}
}
\description{
Calculates malaria incidence at the admin-month level (e.g., district-month)
using a structured cascade framework (N0 through N5) to adjust for testing
gaps, reporting incompleteness, and care-seeking behavior. Facility-level
data is aggregated to admin level before calculating incidence. Returns a
validated dataset with all incidence levels, quality flags, and source
tracking.
}
\details{
The incidence cascade framework:
\itemize{
\item \strong{N0 (Crude Incidence)}:
\itemize{
\item n0_cases = conf
\item n0_incidence = (n0_cases / pop) * rate_multiplier
}
\item \strong{N1 (Testing-Adjusted)}:
\itemize{
\item n1_cases = n0_cases + (pres * tpr)
\item n1_incidence = (n1_cases / pop) * rate_multiplier
}
\item \strong{N2 (Reporting-Adjusted)}:
\itemize{
\item n2_cases = n1_cases / reprate
\item n2_incidence = (n2_cases / pop) * rate_multiplier
}
\item \strong{N3 (Care-Seeking-Adjusted)}:
\itemize{
\item n3_cases = n2_cases + (n2_cases * cs_private/cs_public) +
(n2_cases * cs_none/cs_public)
\item n3_incidence = (n3_cases / pop) * rate_multiplier
}
\item \strong{N4 (Public + Non-Seekers Adjusted)}:
\itemize{
\item n4_cases = n2_cases * (1 + cs_none/cs_public)
\item n4_incidence = (n4_cases / pop) * rate_multiplier
\item \strong{Note}: Excludes private sector adjustment. Use when
private data is already captured in DHIS2 or private burden
is negligible.
\item N4 is always between N2 and N3 in magnitude (N2 < N4 < N3).
}
\item \strong{N5 (Conservative Non-Seekers Adjusted)}:
\itemize{
\item n5_cases = n2_cases * (1 + (cs_none/cs_none_divisor)/cs_public)
\item n5_incidence = (n5_cases / pop) * rate_multiplier
\item \strong{Note}: Like N4 but divides the non-care-seeking proportion
by \code{cs_none_divisor} (default 2) for a more conservative estimate.
\item N5 is always between N2 and N4 in magnitude (N2 < N5 < N4).
}
}

Each level builds on the previous, with N3 representing the most complete
estimate of true community-level malaria incidence. N4 provides a conservative
alternative to N3 that excludes private sector. N5 provides an even more
conservative estimate by reducing the non-seeker adjustment.
}
\examples{
# Example workflow: Calculate TPR first, then incidence
# library(tibble)
#
# # Step 1: Calculate TPR
# facility_data <- tibble(
#   hf_uid = c("HF001", "HF002", "HF003"),
#   adm1 = rep("RegionA", 3),
#   adm2 = rep("DistrictX", 3),
#   date = as.Date(c("2023-01-01", "2023-02-01", "2023-01-01")),
#   conf = c(10, 15, 12),
#   test = c(100, 120, 80),
#   pres = c(5, 8, 10)
# )
#
# tpr_data <- calc_tpr(facility_data)
#
# # Step 2: Add population, reporting rate, and care-seeking data
# tpr_data$pop <- c(5000, 6000, 4500)
# tpr_data$reprate <- c(0.85, 0.90, 0.80)
# tpr_data$cs_public <- 0.60
# tpr_data$cs_private <- 0.25
# tpr_data$cs_none <- 0.15
#
# # Step 3: Calculate incidence (all levels N0-N3)
# result <- calc_incidence(tpr_data)
#
# # Access monthly facility-level data
# result$monthly$hf
#
# # Access monthly district-level data
# result$monthly$adm2
#
# # Access annual facility-level data
# result$annual$hf
#
# # Access annual national-level data
# result$annual$adm0

}
