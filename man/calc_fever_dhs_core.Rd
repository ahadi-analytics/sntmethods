% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_calc_fever.R
\name{calc_fever_dhs_core}
\alias{calc_fever_dhs_core}
\title{Calculate Fever Prevalence from DHS Data}
\usage{
calc_fever_dhs_core(
  dhs_kr,
  survey_vars = list(cluster = "v021", weight = "v005", stratum = "v022", age = "hw1",
    fever = "h22", alive = "b5"),
  region_var = NULL,
  gps_data = NULL,
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM"),
  shapefile = NULL,
  admin_level = NULL,
  join_nearest = TRUE
)
}
\arguments{
\item{dhs_kr}{DHS children's recode (KR) dataset (data.frame or tibble).}

\item{survey_vars}{Named list mapping DHS variable names. Required keys:
\itemize{
\item \code{cluster}: Cluster/PSU ID (default: "v021")
\item \code{weight}: Survey weight (default: "v005")
\item \code{stratum}: Stratum variable (default: "v022")
\item \code{age}: Child's age in months (default: "hw1")
\item \code{fever}: Had fever in last 2 weeks (default: "h22")
\item \code{alive}: Child survival status (default: "b5")
}}

\item{region_var}{Optional column name in \code{dhs_kr} to use as grouping
variable (e.g., "v024" for region).}

\item{gps_data}{Optional DHS GPS dataset with cluster coordinates.}

\item{gps_vars}{Named list for GPS variables (cluster, lat, lon).}

\item{shapefile}{Optional sf object with administrative boundaries.}

\item{admin_level}{Character vector of admin columns from shapefile
(e.g., c("adm1", "adm2")).}

\item{join_nearest}{Logical; if TRUE, assigns clusters outside polygons
to nearest admin unit. Default: TRUE.}
}
\value{
Tibble with fever estimates by grouping level, including:
\itemize{
\item Grouping variables (region, admin level, or national)
\item \code{dhs_fever}: Proportion with fever among U5 children
\item \code{dhs_fever_low}, \code{dhs_fever_upp}: 95\\% confidence interval
\item \code{dhs_n_children}: Number of U5 children (denominator)
\item \code{dhs_n_fever}: Number of febrile children
}
}
\description{
Estimates fever prevalence among children under 5 using survey-weighted
methods. This is step 0 of the case management cascade.
}
\details{
Methodology: \url{https://github.com/ahadi-analytics/sntmethods/blob/master/inst/methods/fever_dhs.yml}

This function calculates fever prevalence among alive U5 children.
The denominator is ALL alive children under 5, not just febrile children.
This differs from CSB/ACT which use febrile children as denominator.

Fever prevalence is the entry point (step 0) of the case management
cascade: Fever -> Sought care -> Tested -> Any antimalarial -> ACT.
}
\examples{
\dontrun{
fever_results <- calc_fever_dhs_core(
  dhs_kr = kr_data,
  region_var = "v024"
)
}

}
\seealso{
\code{\link[=calc_csb_dhs_core]{calc_csb_dhs_core()}} for care-seeking behavior (step 1),
\code{\link[=calc_case_management_dhs]{calc_case_management_dhs()}} for the full cascade
}
