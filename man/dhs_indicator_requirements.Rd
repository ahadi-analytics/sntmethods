% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_preflight.R
\name{dhs_indicator_requirements}
\alias{dhs_indicator_requirements}
\title{Variable requirements per indicator}
\usage{
dhs_indicator_requirements()
}
\value{
A tibble of requirements, one row per indicator.
}
\description{
Mirrors the \code{survey_vars} defaults and the \code{setdiff(needed, names(x))}
checks inside the \verb{calc_*} functions, read forward so a survey can be
assessed before it is processed rather than after it fails.

\code{vars_any} holds alternatives: at least one must be usable. That is how
age is expressed, because DHS records it in \code{hc1} (months, under-fives
only), \code{hml16a} (months, malaria roster) or \code{hml16} (years) depending
on the survey, and an MIS that tested all ages may carry none of the
first.
}
