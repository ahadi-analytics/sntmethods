% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhs_calc_csb_by_wealth_mbg.R
\name{prep_csb_by_wealth_mbg}
\alias{prep_csb_by_wealth_mbg}
\title{Prepare Single CSB Indicator by Wealth Quintile for MBG}
\usage{
prep_csb_by_wealth_mbg(
  dhs_kr,
  gps_data,
  indicator = "public",
  quintiles = 1:5,
  wealth_var = "v190",
  csb_classification = NULL,
  csb_priority_method = c("all", "first", "public", "private"),
  survey_vars = list(cluster = "v001", age = "hw1", fever = "h22"),
  gps_vars = list(cluster = "DHSCLUST", lat = "LATNUM", lon = "LONGNUM")
)
}
\arguments{
\item{dhs_kr}{DHS Children's Recode (KR) dataset.}

\item{gps_data}{DHS GPS dataset with cluster coordinates.}

\item{indicator}{Single indicator name. Default: "public".}

\item{quintiles}{Numeric vector of wealth quintiles to include. Default: 1:5
(all quintiles). Use c(1) for poorest only, c(1,2) for poorest + poorer, etc.}

\item{wealth_var}{Name of wealth quintile variable in dhs_kr. Default: "v190".}

\item{csb_classification}{Data frame with h32 variable to category mapping.
Must have columns \code{variable} and \code{csb}. If NULL, uses default classification.}

\item{csb_priority_method}{Character, one of "all" (default), "first",
"public", or "private". Controls how overlapping care-seeking records
are resolved so each individual is assigned to at most one sector.
See \code{\link[=calc_csb_mbg]{calc_csb_mbg()}} for details. With non-"all" values, csb_public +
csb_private + csb_none sums to 100\% within each quintile.}

\item{survey_vars}{Named list mapping DHS variable names.}

\item{gps_vars}{Named list for GPS variable mapping.}
}
\value{
Named list of data.tables, one per quintile, each with columns:
cluster_id, indicator, samplesize, x, y
}
\description{
Convenience wrapper around \code{\link[=calc_csb_by_wealth_mbg]{calc_csb_by_wealth_mbg()}} to prepare a single
care-seeking indicator stratified by wealth quintile.
}
\examples{
\dontrun{
# Public care-seeking for poorest quintile only
csb_pub_q1 <- prep_csb_by_wealth_mbg(
  dhs_kr = kr_data,
  gps_data = gps_data,
  indicator = "public",
  quintiles = 1
)
}

}
