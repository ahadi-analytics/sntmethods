% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_tpr.R
\name{validate_tpr_proxies}
\alias{validate_tpr_proxies}
\title{Validate TPR Proxy Quality Using Leave-One-Out Cross-Validation}
\usage{
validate_tpr_proxies(
  data,
  hf_var = "hf_uid",
  adm0_var = "adm0",
  adm1_var = "adm1",
  adm2_var = "adm2",
  year_var = "year",
  month_var = "month",
  conf_var = "conf",
  test_var = "test",
  min_facilities = 2,
  generate_plots = TRUE
)
}
\arguments{
\item{data}{Output from \code{calc_tpr()} with \code{include_flags = TRUE}.}

\item{hf_var}{Column name for health facility unique identifier. Should
match the value used in \code{calc_tpr()}. Default is "hf_uid".}

\item{adm0_var}{Column name for national/country level. Default is "adm0".}

\item{adm1_var}{Column name for first administrative level/region. Default
is "adm1".}

\item{adm2_var}{Column name for second administrative level/district.
Default is "adm2".}

\item{year_var}{Column name for year. Default is "year".}

\item{month_var}{Column name for month. Default is "month".}

\item{conf_var}{Column name for confirmed cases. Default is "conf".}

\item{test_var}{Column name for tests. Default is "test".}

\item{min_facilities}{Minimum number of facilities in an admin unit to
include in validation. Default is 2.}

\item{generate_plots}{Logical; if TRUE (default), generates diagnostic
plots. Set to FALSE for metrics only.}
}
\value{
A list containing:
\itemize{
\item \code{metrics}: Data frame with validation metrics for each proxy level
\item \code{validation_data}: Data frame with actual vs proxy comparisons
\item \code{plots}: List of ggplot objects (if generate_plots = TRUE):
\itemize{
\item \code{scatter}: Actual vs proxy scatterplots
\item \code{error_dist}: Error distribution by proxy level
\item \code{mae_by_tests}: MAE vs number of tests
\item \code{stability_map}: Error patterns by test count and TPR value
}
\item \code{summary}: Character vector with key findings
}
}
\description{
Evaluates the accuracy of TPR proxy estimates by comparing them against
actual facility-level TPR values. Uses leave-one-out cross-validation to
calculate what each proxy level (adm2, adm1, prev_year, rolling, adm0)
would have estimated for facility-months with valid raw TPR data.
}
\examples{
# result <- calc_tpr(facility_data, include_flags = TRUE)
# validation <- validate_tpr_proxies(result)
# validation$metrics
# validation$plots$scatter

}
